stages:
  - build

docker-build:
  image: docker:24.0-cli
  services:
    - docker:24.0-dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
  stage: build
  script:
    - docker info
    - docker build -t mock-service:latest .